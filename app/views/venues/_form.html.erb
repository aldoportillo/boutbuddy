<%= form_with(model: venue, html: { class: 'needs-validation', novalidate: true }) do |form| %>
  <% if venue.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <h2><%= pluralize(venue.errors.count, "error") %> prohibited this venue from being saved:</h2>
      <ul>
        <% venue.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-3">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control', placeholder: "MGM Grand Hotel" %>
  </div>

  <!--
  <div class="form-group mb-3">
    <%= form.label :lat, class: 'form-label' %>
    <%= form.text_field :lat, class: 'form-control' %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :lng, class: 'form-label' %>
    <%= form.text_field :lng, class: 'form-control' %>
  </div>
  -->

  <div class="form-group mb-3">
    <%= form.label :address, class: 'form-label' %>
    <%= form.text_field :address, class: 'form-control', placeholder: "3799 S Las Vegas Blvd, Las Vegas, NV, 89109" %>
  </div>

  <%= form.hidden_field :promoter_id, value: current_user.id %>

  <div class="form-group">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
